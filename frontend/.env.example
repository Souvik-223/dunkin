# Spotter Frontend Environment Variables
# In local development, leaving this blank will cause requests to default to /api
# which Vite proxies directly to http://127.0.0.1:8000.
# In production on Vercel, set this to your deployed Render backend API URL.
VITE_API_BASE_URL=https://spotter-backend.onrender.com/api
